# rnw-org
A centralized GitHub repository to host reusable GitHub Actions workflows that can be referenced and shared across all the organization's repositories. Validate the setup by testing the workflows from at least one consuming repository.
